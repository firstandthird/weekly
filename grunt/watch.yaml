options:
  livereload: true
lib:
  files:
    - '<%= jshint.lib %>'
  tasks:
    - 'script-dist'
    - 'mocha'
    - 'notify:generic'
test:
  files:
    - '<%= jshint.test %>'
  tasks:
    - 'mocha'
example:
  files:
    - 'example/*'
build:
  files:
    - 'bower.json'
    - 'Gruntfile.js'
    - 'grunt/*.yaml'
    - 'test/index.html'
  tasks: 'default'
